<h1>New project_link</h1>

<% form_for([@project, @project_link]) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'shared/link_fields', :object=>f %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', project_project_links_path %>